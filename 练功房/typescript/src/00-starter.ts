/**
 * 入门稿：按 materials/a.md「三（2）、ts」顺序自己填空或改错即可。
 * 运行检查：在 练功房 目录执行 npm run typecheck
 */

// --- 1. 基础：给下面变量补上合适类型（不要用 any）---
// const title = "hello";

// --- 2. 联合与收窄：让下面代码在 strict 下通过检查 ---
// function len(x: string | string[]) {
//   return x.length;
// }

// --- 3. 类型守卫：用 typeof / 自定义守卫处理 unknown ---
// function parse(input: unknown) {
//   return String(input);
// }

export {};
